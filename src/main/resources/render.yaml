services:
  - type: web
    name: spring-boot-backend
    env: docker # 🔥 This is the key change
    plan: free
    dockerfilePath: ./Dockerfile # (optional if your Dockerfile is in root)
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
